#include <string>
#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<string> generateParenthesis(int n) {
        std::greater<int>;
        vector<string> v;
        if (n == 1) {
            return v;
        }
        return v;
    }
};

int main() {
    Solution *s = new Solution;
    s->generateParenthesis(3);
    return 0;
}